
public class Movie extends Product implements AllProduct {

	public Movie(int productId, int value, String title, String[] type) {
		super(productId, value, title, type);
		int length;
		double rating;
	}
}
